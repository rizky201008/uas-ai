# UAS AI 2024
Python=3.8

### fastAPi Installation
go to fastApi directory
```shell
cd fastApi 
```

run installation
```shell
pip install -r requirements.txt
```

run server
```shell
uvicorn main:app --reload
```